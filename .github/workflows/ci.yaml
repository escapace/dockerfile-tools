name: ci

on:
  pull_request:
  push:

permissions:
  contents: read

jobs:
  continuous-integration:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v7.0.0
      - name: Set up Go
        uses: actions/setup-go@v6.4.0
        with:
          go-version: 1.26.2
          cache: true
      - name: Run tests
        run: go test ./...
